# CLAUDE.md — قواعد دائمة للمشروع

> هذا الملف ذاكرتي الدائمة لهذا المستودع. أقرأه في بداية كل جلسة وألتزم به.

## 🟦 فرع الرفع الافتراضي

**أرفع دائمًا على الفرع `claude/tender-ramanujan-qPO3L`** (الفرع ذو النقطة الزرقاء
في Replit، وهو الفرع الافتراضي للمستودع). كل عمل/commit/push يذهب إلى هذا الفرع
ما لم يطلب المستخدم صراحةً غير ذلك.

## 🔴 قاعدة إلزامية: فحص الامتثال بعد أي تعديل SEO

**أي تعديل في الـ SEO (العنوان، الوصف، الكلمات المفتاحية، canonical، robots،
Open Graph، البيانات المنظَّمة JSON-LD، الروابط المخفية seo-fallback، sitemap،
manifest، أو أي وسم `<meta>`) يجب أن يتبعه فورًا فحص امتثال قبل الـ commit.**

لا يُعتبر التعديل مكتملًا إلا بعد المرور على القوائم الثلاث التالية والتأكد
أنه لا يخالف أيًّا منها. إن وُجدت مخالفة → أُصلحها قبل المتابعة، أو أُنبّه المستخدم.

---

### ١) امتثال Google AdSense (من سياسات AdSense وملف Auto ads المرفقَين)

- **لا Auto ads إطلاقًا** على هذا الموقع. الإعلانات تظهر فقط عبر **وحدات إعلانية يدوية**،
  و**في صفحات الكبار فقط** (الأدلة/صفحات الآباء) — **ممنوع** أي إعلان في صفحات الأطفال.
- **إعلانات غير مخصّصة (NPA)** دائمًا: يجب بقاء
  `(window.adsbygoogle=window.adsbygoogle||[]).requestNonPersonalizedAds = 1;`
  (التزامًا بسياسة Families / خصوصية الأطفال).
- يبقى معرّف الناشر سليمًا: `client=ca-pub-3132188303904900`.
- **لا ملاحة خادعة (Deceptive navigation):** أي روابط SEO (مثل seo-fallback) يجب أن تشير
  إلى محتوى حقيقي موجود، لا روابط لمحتوى غير موجود ولا إعادة توجيه مضلِّلة.
- لا حشو كلمات مفتاحية ولا محتوى مخادع لتضخيم الزيارات/المشاهدات.
- لا وضع للإعلانات في أماكن غير مسموحة (نوافذ منبثقة، فريمات، إلخ).

#### سياسات Google ادسنس الكاملة المحفوظة (مرجع رسمي في المستودع)

> المصادر الكاملة (PDF) محفوظة في [`docs/adsense-policies/`](docs/adsense-policies/):
> `google-publisher-policies.pdf` (سياسات الناشرين)، `adsense-program-policies-and-managing-sites.pdf`،
> `adsense-site-approvals.pdf` (الموافقة على الموقع)، `adsense-site-issues-faq.pdf`.
> أرجع إليها عند أي عمل خاص بالإعلانات/القبول. ملخّص بنودها الإلزامية:

**أ) سياسات المحتوى (محتوى محظور تمامًا — لا تُعرض معه إعلانات):**
- محتوى غير قانوني أو يشجّع نشاطًا غير قانوني أو ينتهك حقوق الغير.
- انتهاك حقوق الملكية الفكرية / حقوق النشر (DMCA)، أو منتجات مزيّفة/مقلّدة.
- محتوى خطير أو ازدرائي، يحضّ على الكراهية، أو يضايق/يتنمّر/يهدّد.
- محتوى إرهابي أو متطرّف عنيف، أو القسوة في التعامل مع الحيوانات.
- **الاعتداء الجنسي على الأطفال واستغلاله (CSAE)** — حظر مطلق (حسّاس جدًّا لموقع أطفال).
- معلومات مضلِّلة: وصف مضلِّل، ادعاءات غير موثوقة، ملاحة/هوية خادعة، ممارسات احتيالية.
- المواضيع المخصّصة للبالغين **ممنوعة في المحتوى العائلي** (موقعنا عائلي/أطفال → التزام صارم).

**ب) سياسات السلوك:**
- **تصريحات نزيهة:** `ads.txt`/`app-ads.txt` صحيحان (معرّف الناشر `pub-3132188303904900`)؛
  لا بائعون معتمَدون مزيّفون.
- **تداخل/موضع الإعلانات:** ممنوع إعلان يتداخل مع المحتوى أو يُشبه أزرار/عناصر الموقع
  أو يُوضع قرب أزرار بشكل يدفع لنقرات عرضية، أو يقطع الملاحة. (يقابله عندنا: الإعلان
  في قسم ولي الأمر فقط، بعيدًا عن أزرار اللعب، وبعلامة «إعلان».)
- **قيمة الساحة الإعلانية:** لا إعلانات على شاشات/صفحات بلا محتوى ناشر حقيقي، ولا محتوى
  منسوخ/مُكرّر، ولا إعلانات أكثر من المحتوى، ولا لافتات غير معتمدة.
- لا نقرات/مرّات ظهور غير صالحة (لا نقر ذاتي ولا تضخيم اصطناعي).

**ج) الخصوصية + الأطفال:** عدم عرض إعلانات مخصّصة لمن هم دون ١٣ سنة؛ الالتزام بـ COPPA و
  GDPR-K؛ وضع علامة «موجَّه للأطفال» (`tag_for_child_directed_treatment`) على طلب الإعلان.

**د) العقوبات (OFAC):** لا تتوفّر منتجات الناشرين في مناطق خاضعة للعقوبات (شبه جزيرة القرم،
  كوبا، DNR/LNR، إيران، كوريا الشمالية).

**هـ) متطلّبات المحتوى / القبول (مهم لنا — رُصد فعليًّا):** سبق أن عُلِّم الموقع في AdSense بـ
  **«محتوى غير ذي قيمة — يتطلب عناية»** (low-value content). لذلك يجب إبقاء محتوى حقيقي أصلي
  كافٍ وذي قيمة وتنقّل سهل وصفحات غير فارغة (راجع `adsense-site-approvals.pdf` ومتطلّبات
  الحد الأدنى من المحتوى). أي تعديل يقلّل المحتوى الحقيقي أو يضيف صفحات فارغة = مخالفة قبول.


### ٢) شروط عدم الأرشفة / الفهرسة المخزَّنة للمشروع

- الحفاظ على إعدادات الفهرسة المتّفق عليها: `rel="canonical"` الصحيح
  (`https://mykid.oscardevs.com/`) ووسوم `robots` كما هي.
- أي صفحة/قسم متّفق على عدم أرشفته يجب أن يظل غير مؤرشف (عدم إضافة روابط له في
  sitemap أو seo-fallback، والإبقاء على `noindex` إن وُجد).
- عدم تفعيل فهرسة جديدة دون التأكد أنها لا تخالف القواعد أعلاه.

### ٣) مخالفات الفهرسة/الأرشفة في Bing التي رصدناها

> مرجع كامل محفوظ في المستودع: [`docs/bing-webmaster-help-center.md`](docs/bing-webmaster-help-center.md)
> (ملخّص Bing Webmaster Help Center — أرجع إليه عند أي عمل خاص بالفهرسة/الزحف/الـ SEO).
> من أهم بنوده ذات الصلة بنا: العنوان يجب ألّا يكون طويلًا، الروابط يجب ألّا تشير
> إلى محتوى غير موجود («Linking to content that doesn't exist»)، احترام robots.txt
> و canonical و NOINDEX، وأن Bing يقرأ JSON-LD / Open Graph / Microdata.

- **طول العنوان `<title>`:** يجب ألّا يكون كبيرًا — اجعله ضمن **~60 حرفًا** وتجنّب
  بدايته برمز/إيموجي. (هذه مخالفة سبق أن رصدناها في Bing: «العنوان كبير».)
- **طول `<meta name="description">`:** ضمن ~155–160 حرفًا.
- التأكد من عدم تكرار العناوين/الأوصاف وأنها وصفية ومطابقة لمحتوى الصفحة.

---

### خطوات الفحص العملية (بعد كل تعديل SEO)

1. راجع الـ diff: ما الوسوم/الحقول التي تغيّرت؟
2. مرّ على القوائم ①②③ أعلاه بندًا بندًا.
3. تحقّق من العدّادات: طول `<title>` ≤ ~60، طول الوصف ≤ ~160.
4. تأكّد أن كود AdSense (NPA + لا Auto ads + صفحات الكبار فقط) لم يتأثّر.
5. اذكر نتيجة الفحص للمستخدم، وأصلِح أي مخالفة قبل الـ commit.

## خصوصية

- لا تُخزَّن أي بيانات شخصية (صور هوية، أرقام قومية، أسماء/عناوين أشخاص) في
  المستودع أو في أي ملف. ملف الهوية المرفوع سابقًا = بيانات شخصية، لا يُحفظ ولا يُنسخ.
