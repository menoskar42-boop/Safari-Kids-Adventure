# CLAUDE.md — قواعد دائمة للمشروع

> هذا الملف ذاكرتي الدائمة لهذا المستودع. أقرأه في بداية كل جلسة وألتزم به.

## 🔴 قاعدة إلزامية: فحص الامتثال بعد أي تعديل SEO

**أي تعديل في الـ SEO (العنوان، الوصف، الكلمات المفتاحية، canonical، robots،
Open Graph، البيانات المنظَّمة JSON-LD، الروابط المخفية seo-fallback، sitemap،
manifest، أو أي وسم `<meta>`) يجب أن يتبعه فورًا فحص امتثال قبل الـ commit.**

لا يُعتبر التعديل مكتملًا إلا بعد المرور على القوائم الثلاث التالية والتأكد
أنه لا يخالف أيًّا منها. إن وُجدت مخالفة → أُصلحها قبل المتابعة، أو أُنبّه المستخدم.

---

### ١) امتثال Google AdSense (من سياسات AdSense وملف Auto ads المرفقَين)

- **لا Auto ads إطلاقًا** على هذا الموقع. الإعلانات تظهر فقط عبر **وحدات إعلانية يدوية**،
  و**في صفحات الكبار فقط** (الأدلة/صفحات الآباء) — **ممنوع** أي إعلان في صفحات الأطفال.
- **إعلانات غير مخصّصة (NPA)** دائمًا: يجب بقاء
  `(window.adsbygoogle=window.adsbygoogle||[]).requestNonPersonalizedAds = 1;`
  (التزامًا بسياسة Families / خصوصية الأطفال).
- يبقى معرّف الناشر سليمًا: `client=ca-pub-3132188303904900`.
- **لا ملاحة خادعة (Deceptive navigation):** أي روابط SEO (مثل seo-fallback) يجب أن تشير
  إلى محتوى حقيقي موجود، لا روابط لمحتوى غير موجود ولا إعادة توجيه مضلِّلة.
- لا حشو كلمات مفتاحية ولا محتوى مخادع لتضخيم الزيارات/المشاهدات.
- لا وضع للإعلانات في أماكن غير مسموحة (نوافذ منبثقة، فريمات، إلخ).

### ٢) شروط عدم الأرشفة / الفهرسة المخزَّنة للمشروع

- الحفاظ على إعدادات الفهرسة المتّفق عليها: `rel="canonical"` الصحيح
  (`https://mykid.oscardevs.com/`) ووسوم `robots` كما هي.
- أي صفحة/قسم متّفق على عدم أرشفته يجب أن يظل غير مؤرشف (عدم إضافة روابط له في
  sitemap أو seo-fallback، والإبقاء على `noindex` إن وُجد).
- عدم تفعيل فهرسة جديدة دون التأكد أنها لا تخالف القواعد أعلاه.

### ٣) مخالفات الفهرسة/الأرشفة في Bing التي رصدناها

- **طول العنوان `<title>`:** يجب ألّا يكون كبيرًا — اجعله ضمن **~60 حرفًا** وتجنّب
  بدايته برمز/إيموجي. (هذه مخالفة سبق أن رصدناها في Bing: «العنوان كبير».)
- **طول `<meta name="description">`:** ضمن ~155–160 حرفًا.
- التأكد من عدم تكرار العناوين/الأوصاف وأنها وصفية ومطابقة لمحتوى الصفحة.

---

### خطوات الفحص العملية (بعد كل تعديل SEO)

1. راجع الـ diff: ما الوسوم/الحقول التي تغيّرت؟
2. مرّ على القوائم ①②③ أعلاه بندًا بندًا.
3. تحقّق من العدّادات: طول `<title>` ≤ ~60، طول الوصف ≤ ~160.
4. تأكّد أن كود AdSense (NPA + لا Auto ads + صفحات الكبار فقط) لم يتأثّر.
5. اذكر نتيجة الفحص للمستخدم، وأصلِح أي مخالفة قبل الـ commit.

## خصوصية

- لا تُخزَّن أي بيانات شخصية (صور هوية، أرقام قومية، أسماء/عناوين أشخاص) في
  المستودع أو في أي ملف. ملف الهوية المرفوع سابقًا = بيانات شخصية، لا يُحفظ ولا يُنسخ.
